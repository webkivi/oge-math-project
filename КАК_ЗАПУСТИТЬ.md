# КАК ЗАПУСТИТЬ ПРОЕКТ

Это полная папка проекта. Всё уже разложено по местам.

## Шаг 1. Распакуй этот архив

Создай на компьютере папку, например:
  C:\Проекты\oge-math

Распакуй архив туда. Внутри должны оказаться файлы и папки прямо в oge-math,
а не вложенная ещё одна папка. То есть так:

  oge-math\CLAUDE.md              ← этот файл видно сразу
  oge-math\Methodology_v2_1.md
  oge-math\.claude\               ← папка с точкой в начале (это нормально)
  oge-math\content\               ← уроки
  ...и т.д.

Если после распаковки внутри oge-math лежит ещё одна папка project-ready —
перетащи всё из неё на уровень выше, а пустую project-ready удали.

Важно: папка .claude начинается с точки. Windows может её прятать.
Если не видишь — в проводнике нажми «Вид → Показать скрытые элементы».

## Шаг 2. Открой в Claude Code

Открой приложение Claude Code. Нажми «Open folder» (или «Открыть папку»)
и выбери oge-math. Claude Code прочитает CLAUDE.md и поймёт, что это за проект.

## Шаг 3. Проверь, что всё работает

Напиши в окне обычными словами:

  Прочитай CLAUDE.md и расскажи, как устроен проект.

Если Claude ответил по делу (упомянул ОГЭ, PWA, 4 роли) — всё работает.

## Шаг 4. Начинай работу

Первая настоящая задача по плану:

  /фундамент

Это команда, которая запустит проектирование ядра: как ученик проходит урок.
Claude сам вызовет нужных «агентов», проверит результат и покажет тебе итог.

## Что внутри (не надо открывать и менять руками)

  .claude/          — инструкции для Claude Code (агенты, команды, проверки)
  tools/            — скрипт-проверщик validator.py
  prompts/          — тексты ролей агентов (уже синхронизированы под v2.1/v3.2)
  content/          — уроки 1.1–1.9 в CSV
  fipi/             — документы ФИПИ (кодификатор, спецификация, демоверсия)
  specs/            — сюда Claude будет класть архитектурные спецификации
  CLAUDE.md         — главные правила для Claude Code
  Methodology_v2_1  — конституция продукта
  Project_Brain_v3_2 — стратегия и статус

---

# ЗАПУСК ПРИЛОЖЕНИЯ ЛОКАЛЬНО (first-run)

Вертикальный срез регистрации можно поднять и пройти вживую: бэкенд (FastAPI) +
фронтенд (PWA на Vite). Бэкенд запускается в WSL2 (там venv с зависимостями),
фронтенд — где удобно (Windows или WSL).

## 1. Бэкенд (FastAPI, порт 8000) — в WSL2, из корня проекта

```bash
# первый раз — создать venv (если его ещё нет) и поставить зависимости:
python3 -m venv .venv          # пропусти, если .venv уже есть
.venv/bin/python -m pip install -r backend/requirements.txt

# запуск. APP_ENV=staging — чтобы класс 8 принимался так же, как в dev-фронтенде
# (для класса 9 значение не важно). Таблицы БД создаются при старте автоматически
# (вне production). Без APP_ENV (=production) таблицы НЕ создаются — нужен APP_ENV.
APP_ENV=staging .venv/bin/python -m uvicorn backend.main:app --reload --port 8000
```

Проверка: открой http://localhost:8000/api/pd-policy — вернётся JSON с версией политики.

## 2. Фронтенд (Vite, порт 5173) — из папки frontend/

```bash
npm install        # первый раз
npm run dev        # http://localhost:5173 ; запросы /api проксируются на :8000
```

## 3. Пройди регистрацию

Открой http://localhost:5173 и пройди:
**имя → класс «9» → согласие на Политику → «Начать»** → экран «Готово 🙂».

Под капотом: фронтенд держит черновик в памяти, на «Начать» шлёт один запрос
`POST /api/registration` с заголовком `Idempotency-Key`; бэкенд создаёт аккаунт и
ставит httpOnly-cookie сессии. Первый урок (daily_start) — следующий этап (v4),
сейчас на его месте заглушка.

> Если страница открывается, но регистрация не уходит — проверь, что бэкенд запущен
> на :8000 (шаг 1) и доступен по http://localhost:8000/api/pd-policy. На Windows+WSL2
> localhost проброшен в обе стороны; если прокси не достаёт бэкенд — запусти оба
> процесса в одной среде (оба в WSL).

## Тесты (Definition of Done)

```bash
# бэкенд — из корня проекта (WSL):
.venv/bin/python -m pytest backend/tests -q
# фронтенд — из папки frontend:
cd frontend && npm run build && npm run lint && npm run test
```
